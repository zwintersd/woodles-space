[project]
name = "woodles-animations"
version = "0.1.0"
description = "Manim animation playspace for woodles.space"
requires-python = ">=3.12"
dependencies = [
    "manim>=0.19.0",
]

[tool.uv]
dev-dependencies = []
